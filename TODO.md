# Git Fix TODO

## Steps

- [x] Step 1: Remove duplicate `web/` folder from git (files already exist in `IAPR_Web/`)
- [x] Step 2: Commit and push the removal to `blackboxai/github-pages-demo`
- [x] Step 3: Fix broken git worktree (`gh-pages-new`) - removed with `git worktree remove --force`
- [x] Step 4: Fix `gh-pages` local branch - deleted local branch (managed by GitHub Actions)
- [x] Step 5: Added `web/` to `.gitignore` to prevent accidental re-commit

## All done ✓
